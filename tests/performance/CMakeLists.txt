function(add_ms_bench name source)
    add_executable(${name} ${source})
    target_link_libraries(${name} PRIVATE mathscript benchmark::benchmark benchmark::benchmark_main)
    target_include_directories(${name} PRIVATE
        ${CMAKE_SOURCE_DIR}/include
        ${CMAKE_BINARY_DIR}/include
    )
endfunction()

file(GLOB_RECURSE _MS_BENCH_SOURCES RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}"
    "bench_*.cpp")
list(SORT _MS_BENCH_SOURCES)
foreach(_src ${_MS_BENCH_SOURCES})
    get_filename_component(_stem ${_src} NAME_WE)
    add_ms_bench(${_stem} ${_src})
endforeach()
if(TARGET bench_repl)
    target_link_libraries(bench_repl PRIVATE ms_interp)
endif()
if(TARGET bench_special)
    target_link_libraries(bench_special PRIVATE ms_special)
endif()
if(TARGET bench_special_memory)
    target_link_libraries(bench_special_memory PRIVATE ms_special)
endif()
if(TARGET bench_fem)
    target_link_libraries(bench_fem PRIVATE ms_fem)
endif()
if(TARGET bench_tensorops)
    target_link_libraries(bench_tensorops PRIVATE ms_tensorops)
endif()
if(TARGET bench_topo)
    target_link_libraries(bench_topo PRIVATE ms_topo)
endif()
